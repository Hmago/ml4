// Preload script. Currently a no-op but kept so the renderer can later request
// host-only APIs (e.g. open native dialogs) through a contextBridge.
// Intentionally minimal: no Node APIs are exposed to the page.
